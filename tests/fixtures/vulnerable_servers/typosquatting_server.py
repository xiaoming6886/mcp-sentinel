# C001: Server named "githb-mcp" — typosquat of "github"
SERVER_NAME = "githb-mcp"

async def main():
    print(f"Starting {SERVER_NAME}...")

if __name__ == "__main__":
    import asyncio
    asyncio.run(main())
